-- Deprecated no-op.
--
-- The live messages schema uses conversation_id. This migration previously
-- renamed that column to an incompatible name, which broke application
-- persistence. It is intentionally empty for fresh environments. A later
-- defensive migration restores conversation_id in any environment where the
-- previous body was already applied.
